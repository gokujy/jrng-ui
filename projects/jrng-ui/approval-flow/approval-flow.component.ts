import { DatePipe, NgTemplateOutlet } from '@angular/common';
import { ChangeDetectionStrategy, Component, TemplateRef, contentChild, input } from '@angular/core';
export type JApprovalStatus='pending'|'approved'|'rejected'|'skipped'|'cancelled';
export type JApprovalLayout='horizontal'|'vertical'|'compact';
export interface JApprovalStep<T=unknown>{readonly id:string|number;readonly label:string;readonly status:JApprovalStatus;readonly actor?:string;readonly timestamp?:Date|string|number;readonly comments?:string;readonly optional?:boolean;readonly group?:string;readonly data?:T;}
@Component({selector:'j-approval-flow',imports:[DatePipe,NgTemplateOutlet],template:`<ol class="j-approval-flow" [class.j-approval-flow--horizontal]="layout()==='horizontal'" [class.j-approval-flow--compact]="layout()==='compact'" [attr.aria-label]="ariaLabel()">@for(step of steps();track step.id;let i=$index){<li [attr.data-status]="step.status" [attr.aria-current]="isCurrent(step,i)?'step':null"><span class="j-approval-flow__marker" aria-hidden="true">{{marker(step.status)}}</span><article><strong>{{step.label}}</strong>@if(step.optional){<small>Optional</small>}<p>{{label(step.status)}}</p>@if(step.actor){<span>{{step.actor}}</span>}@if(step.timestamp){<time>{{step.timestamp|date:'medium'}}</time>}@if(step.comments){<blockquote>{{step.comments}}</blockquote>}@if(actionTemplate();as template){<ng-container [ngTemplateOutlet]="template" [ngTemplateOutletContext]="{$implicit:step,index:i}"/>}</article></li>}</ol>`,styles:[`.j-approval-flow{display:grid;gap:var(--j-spacing-3);list-style:none;margin:0;padding:0}.j-approval-flow li{display:grid;grid-template-columns:2rem 1fr;gap:var(--j-spacing-2);position:relative}.j-approval-flow__marker{align-items:center;border:2px solid var(--j-color-border);border-radius:50%;display:flex;height:1.75rem;justify-content:center;width:1.75rem}.j-approval-flow li[data-status=approved] .j-approval-flow__marker{border-color:var(--j-color-success);color:var(--j-color-success)}.j-approval-flow li[data-status=rejected] .j-approval-flow__marker{border-color:var(--j-color-danger);color:var(--j-color-danger)}article{display:grid;gap:var(--j-spacing-1)}p,time,blockquote{margin:0;color:var(--j-color-muted-foreground)}.j-approval-flow--horizontal{grid-auto-flow:column;grid-auto-columns:minmax(10rem,1fr);overflow:auto}.j-approval-flow--horizontal li{grid-template-columns:1fr;text-align:center}.j-approval-flow--horizontal .j-approval-flow__marker{margin:auto}.j-approval-flow--compact article p,.j-approval-flow--compact blockquote{display:none}@media(max-width:48rem){.j-approval-flow--horizontal{grid-auto-flow:row}}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class JApprovalFlowComponent<T=unknown>{readonly steps=input<readonly JApprovalStep<T>[]>([]);readonly layout=input<JApprovalLayout>('vertical');readonly mode=input<'sequential'|'parallel'>('sequential');readonly readonly=input(true);readonly currentStep=input<string|number|null>(null);readonly ariaLabel=input('Approval flow');readonly actionTemplate=contentChild<unknown,TemplateRef<unknown>>('jApprovalActions',{read:TemplateRef});isCurrent(s:JApprovalStep<T>,i:number){return this.currentStep()!=null?this.currentStep()===s.id:s.status==='pending'&&(this.mode()==='parallel'||!this.steps().slice(0,i).some(x=>x.status==='pending'));}marker(s:JApprovalStatus){return({pending:'…',approved:'✓',rejected:'×',skipped:'–',cancelled:'×'} as const)[s];}label(s:JApprovalStatus){return s[0]?.toUpperCase()+s.slice(1);}}
