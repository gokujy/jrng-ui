/**
 * Compatibility entry point for the original `jrng-ui/image` import path.
 * New code may import the same components from `jrng-ui/image-preview`.
 */
export { JImageComponent, JImagePreviewComponent } from 'jrng-ui/image-preview';
