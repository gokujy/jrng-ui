# Public directives, pipes, and services

Generated from built public declarations. Each record is covered by the strict entrypoint and package gates.

## Directives

### JAutoFocusDirective

- Import: import { JAutoFocusDirective } from 'jrng-ui/core';
- Selector: [jAutoFocus]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JClickOutsideDirective

- Import: import { JClickOutsideDirective } from 'jrng-ui/core';
- Selector: [jClickOutside]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JCopyToClipboardDirective

- Import: import { JCopyToClipboardDirective } from 'jrng-ui/core';
- Selector: [jCopyToClipboard]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JFocusTrapDirective

- Import: import { JFocusTrapDirective } from 'jrng-ui/core';
- Selector: [jFocusTrap]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JFormSubmitDirective

- Import: import { JFormSubmitDirective } from 'jrng-ui/form-submit';
- Selector: form[jFormSubmit]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JHotkeyDirective

- Import: import { JHotkeyDirective } from 'jrng-ui/core';
- Selector: [jHotkey]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JIntersectionObserverDirective

- Import: import { JIntersectionObserverDirective } from 'jrng-ui/core';
- Selector: [jIntersectionObserver]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JKeyFilterDirective

- Import: import { JKeyFilterDirective } from 'jrng-ui/key-filter';
- Selector: [jKeyFilter]
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JResizeObserverDirective

- Import: import { JResizeObserverDirective } from 'jrng-ui/core';
- Selector: [jResizeObserver]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JRippleDirective

- Import: import { JRippleDirective } from 'jrng-ui/core';
- Selector: [jRipple]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTableActionsTemplateDirective

- Import: import { JTableActionsTemplateDirective } from 'jrng-ui/table';
- Selector: ng-template[jTableActions]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTableCellTemplateDirective

- Import: import { JTableCellTemplateDirective } from 'jrng-ui/table';
- Selector: ng-template[jTableCell]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTableEmptyTemplateDirective

- Import: import { JTableEmptyTemplateDirective } from 'jrng-ui/table';
- Selector: ng-template[jTableEmpty]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTableFilterTemplateDirective

- Import: import { JTableFilterTemplateDirective } from 'jrng-ui/table';
- Selector: ng-template[jTableFilter]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTableHeaderTemplateDirective

- Import: import { JTableHeaderTemplateDirective } from 'jrng-ui/table';
- Selector: ng-template[jTableHeader]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTableLoadingTemplateDirective

- Import: import { JTableLoadingTemplateDirective } from 'jrng-ui/table';
- Selector: ng-template[jTableLoading]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTooltipDirective

- Import: import { JTooltipDirective } from 'jrng-ui/tooltip';
- Selector: [jTooltip]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTourStepDirective

- Import: import { JTourStepDirective } from 'jrng-ui/tour';
- Selector: [jTourStep]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTreeTableCellTemplateDirective

- Import: import { JTreeTableCellTemplateDirective } from 'jrng-ui/tree-table';
- Selector: ng-template[jTreeTableCell]
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: semantic behavior is part of the directive/component contract
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

## Pipes

### JAccountingCurrencyPipe

- Import: import { JAccountingCurrencyPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JBooleanLabelPipe

- Import: import { JBooleanLabelPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JBytesPipe

- Import: import { JBytesPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JCompactNumberPipe

- Import: import { JCompactNumberPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JCurrencyFormatPipe

- Import: import { JCurrencyFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JDateFormatPipe

- Import: import { JDateFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JDateTimeFormatPipe

- Import: import { JDateTimeFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JDefaultTextPipe

- Import: import { JDefaultTextPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JDurationPipe

- Import: import { JDurationPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JFileExtensionPipe

- Import: import { JFileExtensionPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JFileSizeFormatPipe

- Import: import { JFileSizeFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JInitialsPipe

- Import: import { JInitialsPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JJoinValuesPipe

- Import: import { JJoinValuesPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JJsonDisplayPipe

- Import: import { JJsonDisplayPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JMaskedEmailPipe

- Import: import { JMaskedEmailPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JMaskedPhonePipe

- Import: import { JMaskedPhonePipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JNumberFormatPipe

- Import: import { JNumberFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JPercentFormatPipe

- Import: import { JPercentFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JPluralizePipe

- Import: import { JPluralizePipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JRelativeTimePipe

- Import: import { JRelativeTimePipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JSearchHighlightPipe

- Import: import { JSearchHighlightPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTextTruncatePipe

- Import: import { JTextTruncatePipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTimeFormatPipe

- Import: import { JTimeFormatPipe } from 'jrng-ui/formatting';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

## Services

### JBodyScrollLockService

- Import: import { JBodyScrollLockService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JClipboardService

- Import: import { JClipboardService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JConfirmationService

- Import: import { JConfirmationService } from 'jrng-ui/confirm-dialog';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JDialogService

- Import: import { JDialogService } from 'jrng-ui/dialog';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JDownloadService

- Import: import { JDownloadService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JEditorCommandService

- Import: import { JEditorCommandService } from 'jrng-ui/editor';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JFullscreenService

- Import: import { JFullscreenService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JIconRegistry

- Import: import { JIconRegistry } from 'jrng-ui/icon';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JIdGenerator

- Import: import { JIdGenerator } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JKeyboardShortcutsService

- Import: import { JKeyboardShortcutsService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JLiveAnnouncerService

- Import: import { JLiveAnnouncerService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JMediaQueryService

- Import: import { JMediaQueryService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JOverlayService

- Import: import { JOverlayService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JOverlayStackService

- Import: import { JOverlayStackService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JPrintService

- Import: import { JPrintService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JStorageService

- Import: import { JStorageService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JThemeService

- Import: import { JThemeService } from 'jrng-ui/theming';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JToastService

- Import: import { JToastService } from 'jrng-ui/toast';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: no detected browser-only API
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JTourService

- Import: import { JTourService } from 'jrng-ui/tour';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.

### JZIndexManagerService

- Import: import { JZIndexManagerService } from 'jrng-ui/core';
- Provider: root or documented component provider
- Test status: covered
- SSR behavior: feature-detected browser APIs; unsupported environments use safe fallbacks
- Accessibility/keyboard: not directly interactive
- Errors: recoverable operations return/emit explicit failure state where applicable.
- Example: import from the modular entrypoint and use the typed public API; see generated declarations for methods, inputs, outputs, null and locale behavior.
